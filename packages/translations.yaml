# Compile-time language selector. The selected catalog is a package fragment
# with a concrete substitutions mapping, so packages never receives an
# IncludeFile as the value of substitutions.
packages:
  selected_language: !include translations/${language}.yaml
